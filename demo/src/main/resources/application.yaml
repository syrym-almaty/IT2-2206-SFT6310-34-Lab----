spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/zhak
    driverClassName: org.postgresql.Driver
    username: zhako
    password: zhako

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: name
